{% extends 'layouts/logged_in.twig' %}

{% block page_content %}

<h1 class="h2 mb-4">
  {{title}}
</h1>

{% if exists == 1 %}
    <form enctype="multipart/form-data" method="post">
    
    <div class="row">
		{# This hidden div is in development #}
        <div id="groupAvailability" class="col-12 col-lg-6" style="display: none;">
            Not visible!
        </div>
		<div id="myTimeSelector" class="col-12 col-lg-6" style="display: inline-block;">
            {{ include('schedule/_available_selector.twig') }}
        </div>
        <div class="col-12 col-lg-6">
            {% if is_anon == 0 %}
                {{ include('schedule/_available_display_invite_page.twig') }}
            {% endif %}
        </div>
    </div>
    
    <div class="pt-4">
    <button class="btn btn-success">
        Save Schedule
    </button>
    <a class="ml-3" href="{{site_url}}/schedule">
        Cancel
    </a>
    </div>

    </form>

{% endif %}


{% endblock %}
