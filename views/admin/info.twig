{% extends 'layouts/logged_in.twig' %}

{% block page_content %}

<div class="align-items-center d-md-flex justify-content-between">
  <h1 class="h2 mb-4 col-6">
    {{title}}
  </h1>
</div>

{% if isadmin != 0 %}
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav col-12">
        <a class="nav-link col" href="{{site_url}}/adminusers">Users</a>
        <a class="nav-link col" href="{{site_url}}/adminevents">Events</a>
        <a class="nav-link active col" href="{{site_url}}/admininfo">Info</a>
        <a class="hidden col-4"></a>
      </div>
    </div>
  </nav>
  <div class="row">
    <div class="col">
      <div class="tab-content pt-3">
        <div class="tab-pane fade show active" id="users" role="tabpanel" aria-labelledby="users-tab">
          This page is not implemented yet. It will have orphaned data counters in the future.
        </div>
      </div>
    </div>
  </div>
{% else %}
  <p>You do not have access to this page.</p>
{% endif %}

{% endblock %}
